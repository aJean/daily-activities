import { INodeProps } from '../types';

export const DEFAULT_BG_COLOR: string = '#F7F7F7';

export const DEFAULT_LINE_COLOR: string = '#999999';

export const DEFAULT_SPACE_X: number = 16;

export const DEFAULT_SPACE_Y: number = 16;

export const DEFAULT_NODE_PROPS: INodeProps = {
  isEditing: false,
  invalid: false,
};
